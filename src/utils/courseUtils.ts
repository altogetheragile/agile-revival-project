/**
 * Course utility functions for handling course-related operations
 */

/**
 * Creates a consistent registration ID for a course
 * Note: This is kept for reference but we're now using the direct course ID
 * 
 * @param courseId The original course ID
 * @returns A formatted ID for use with registrations
 */
export const createCourseRegistrationId = (courseId: string): string => {
  return courseId;
};
